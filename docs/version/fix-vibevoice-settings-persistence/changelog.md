# Fix: 任务"原文/全文转录"内容为空 - Change Log

**Branch**: `fix/vibevoice-settings-persistence`
**Date**: 2026-08-08

## Change Log

- 2026-08-08: 修复"任务已完成但原文/全文转录内容为空、无法复制"的回归 bug。根因：commit `7293823`（perf: avoid loading full multipart summaries on select）将任务选中时的详情请求改为 `GET /tasks/{id}?include_content=false`，后端在该模式下剥离 `transcript` 字段；而唯一加载完整内容（`include_content=true`）的入口 `fetchTaskFullContent` 只挂在 multipart 任务的"展开完整分P总结"按钮上，导致所有历史已完成任务选中后 `transcript` 恒为 null（"AI 总结"tab 正常，因为后端保留截断后的 summary）。修复方案（保持轻量加载性能优化不变）：前端 `useTaskViewModel.ts` 新增三条转录按需加载链路——① watch `activeTab`，切换到"原文"tab 且 transcript 为空时调用 `fetchTaskFullContent` 懒加载完整内容；② `selectTask` 详情响应合并时保留已加载的 transcript，防止轻量响应覆盖；③ `copyContent('transcript')` 在 transcript 为空时先加载完整内容再复制（任务确实无转录时仍返回失败提示）。数据库与后端接口无问题（已验证目标任务 `c54fa795` transcript 完整 5416 字符，`include_content=true` 路径完好）。
- 2026-08-08: 新增测试覆盖：前端 `useTaskViewModel.transcript.test.ts`（5 个用例：selectTask 轻量请求 / 切"原文"tab 触发完整加载 / 已加载 transcript 不被覆盖 / 复制前先加载 / 无转录时返回 false）；后端 `tests/test_task_include_content.py`（4 个用例：默认返回完整内容 / `include_content=false` 剥离 transcript 与 summary_meta / 超长 summary 截断 / 分P标记处截断）。
- 2026-08-08: 新增任务排队可视化后端支撑（分支内另一 feature 功能变更，随当前分支发布）——① Worker 基类新增 `snapshot()`（返回 name/active_task_id/waiting_task_ids/queue_size，FIFO 顺序，异常时安全默认）；② 新增 `GET /tasks/queue` 返回 4 个固定队列（VideoDownloaderWorker/FileUploadWorker/TranscriberWorker/LLMWorker）快照 + UTC 时间戳，未实例化 worker 返回全空快照；③ WS `task_update` 消息扩展 `queues` 字段（向后兼容，旧客户端忽略）；④ lifespan 启动 pipeline 前调用 `db.recover_interrupted_tasks()`（同步方法，非 await）将中断任务标记为 FAILED；⑤ `POST /tasks/` 内存 TTL 幂等去重：5 秒内同一 video_url 重复提交直接返回已创建任务完整数据（201），多分P separate 模式跳过，文件上传路径不处理；⑥ domain/task/type.py `TaskStatus` 补 `PARTIAL` 枚举（与 db.py 8 值对齐）。后端单测新增 5 个文件（worker 快照 / queue 路由 / WS queues / lifespan 恢复接线 / 提交去重）共 25 用例。
- 2026-08-08: 前端排队可视化——① `types.ts` 新增 `QueueSnapshot`/`QueueResponse` 契约；② `useTaskViewModel` 新增 `queues` 状态与 `fetchQueueSnapshot()`，WS `task_update` 解析可选 `queues` 字段自动刷新；③ Sidebar 排队任务显示琥珀色徽章「排队中 (下载/上传/转录/总结队列 #N)」并抑制进度条（`utils/queueStatus.ts` 纯函数推导）；④ TaskInfoModal 补齐 UPLOADING='上传中'/PARTIAL='部分完成' 映射；⑤ 修复 types.test.ts 断言为 8 值（原 CI 红）。前端单测新增 2 文件 11 用例。
- 2026-08-08: **修复存量严重 bug：pipeline 事件重复订阅**——主应用（ShengWen-app.py）与挂载子应用（api.py）各自的 lifespan 都调用 `pipeline.start()`，导致 TASK_CREATED 被重复订阅、每个任务被派发两次（下载/上传 worker 队列出现两个相同条目）。文件任务第二个条目处理时源文件已被移走 → 误标 FAILED（即使第一个条目正在正常转录/排队）；URL 任务重复下载。修复：`pipeline.start()/stop()` 幂等化（重复调用不再重复订阅）。单测 4 用例（tests/test_pipeline_idempotent.py）。该 bug 由 e2e 队列快照观察发现（修复前 waiting 出现重复条目与 FAILED 排队任务）。
- 2026-08-08: 【遗留问题】`temp/` 目录（约 4.6GB / 633 文件）无任何回收机制：下载的视频、FileUploadWorker 移动后的任务文件、转录中间产物永久留存。需后续设计按任务生命周期的清理策略。
- 2026-08-08: 回收机制 e2e 验证中发现并修复两个回收器 bug——① **B站 multipart 分P文件匹配错误**：`resolve_task_media_files` 的 multipart 匹配误放在非 B站（else）分支，B站任务（video_url 含 BV）的分P文件（`{task_id}_p{N}`）与 BV 命名分P（`{BVid}_p{N}`）永远匹配不到 → 回收死锁（超限但 0 删除，2.4GB 分P无法回收）。修复：multipart 匹配移出分支判断，对所有任务适用并补充 BV 命名分P匹配。② **孤儿媒体文件不回收**：任务已删除的 `{task_id}.ext`、无任务引用的 `{BVid}.mp4/.mp3`、BV 命名分P文件不在孤儿清扫范围（原只清 `*_temp.*` 与 `*.part/.ytdl`），永久残留。修复：`list_orphans` 扩展媒体文件孤儿判断（无任务引用且超期可回收，有任务引用的 BV 保守保留）。新增回归测试 3 个（B站 multipart 解析 / 上传任务不受影响 / 孤儿媒体判定），e2e 实测 temp 3.0GB → 0.1MB（释放 2.48GB）。
- 2026-08-08: **temp/ 存储回收机制 + 音频状态字段 + 重新下载**（分支内另一 feature 功能变更，随当前分支发布）——① 配置：`storage.max_total_mb` 默认 2048→10240（10G，settings.py 默认值 + config/settings.json 同步）；② tasks 表新增 `audio_downloaded` BOOLEAN / `audio_missing_reason` VARCHAR（db.py TaskModel + to_dict + `_ensure_schema` 幂等 ALTER）；③ 独立 asyncio 周期任务 `StorageReclaimerLoop`（domain/storage/reclaimer_loop.py 新增），lifespan（api.py 与 ShengWen-app.py 两处）在 recover_interrupted_tasks 之后执行 `start_storage_reclaimer()`（先 backfill_legacy 存量推断回填、不广播，再启动循环），关停 `stop_storage_reclaimer()`；④ `StorageReclaimService.run_once()`：回收单元=任务文件组，按任务 created_at FIFO，两档回收（第一档媒体文件：B站 `{BVid}.mp4/.mp3/.webm/.mkv/.part`、上传 `{task_id}{ext}/.mp3/_temp{ext}`、multipart `{task_id}_p{N}.*`；第二档中间产物 `{task_id}_subtitle.txt/*_summary.*/*_re.*/*_multipart*.txt/*_overview*.txt/{BVid}_summary.*`）；资格=终态（COMPLETED/PARTIAL/FAILED）+ 超过保留期（COMPLETED/PARTIAL 86400s、FAILED 7200s）+ 不在 4 worker 快照 active∪waiting 集合（api.get_queue_snapshots 惰性导入避免循环）；共享 BV 文件引用计数：仅当全部引用任务都在本次回收集合才删；孤儿清扫 `*_temp.*`（无对应任务或终态且超期）与 `*.part/.ytdl`（超 1h）；删媒体文件后 `update_and_notify({"audio_downloaded": false, "audio_missing_reason": "reclaimed"})`（先删后标记、每任务一次），只删中间产物不标记；⑤ 写入时机 4 处：video_downloader_worker 下载成功与 file_upload_worker 移动成功后置 True，字幕直取两处（`_try_process_with_bilibili_subtitle`/`_try_process_bilibili_multi_part_merge`）置 False+subtitle_only；⑥ `POST /tasks/{task_id}/re-download` 新端点（校验矩阵：404 不存在 / 400 file:// 本地任务 / 409 无 transcript 引导 re-transcribe / 409 本地已有媒体文件），合法时置 DOWNLOADING 并派发 `{task_id, video_url, quality: audio_only, summary_mode, re_download_only: True, restore_status}`，downloader 新增 re_download_only 分支（下载成功后恢复原状态并 return，跳过字幕直取与 next_worker 派发）；⑦ 废弃代码清理：domain/storage 的 FileType/CleanupTrigger/FileRecord/StoragePolicy 重写为 AudioMissingReason/TaskFileGroup/ReclaimResult，repository 重写为 TempFileRepository，service 重写为 StorageReclaimService，删除纯内存死代码 infra/storage/local_storage.py；⑧ 单测：新增 test_temp_reclaimer.py（15 用例：队列顺序/保护期/在跑保护/两档优先级/字段标记/共享BV/孤儿/边界/循环启停）、test_audio_status.py（6 用例：infer 三分支 + backfill 不广播）、test_re_download_route.py（5 用例：404/400/409×2/200+payload 断言）；重写 test_storage_service.py/test_storage_type.py；test_config.py 更新 max_total_mb==10240；test_lifespan_recover.py 隔离回收器 mock 避免对真实 DB/temp 产生副作用。前端（本分支另一子任务）：Task 增加 audio_downloaded/audio_missing_reason，TaskInfoModal 状态徽章与「重新下载」按钮，FloatingToolbar「原文」下拉加「重新下载音频」，useTaskViewModel.reDownloadAudio 接线 App.vue。
- 2026-08-08: **修复"查看原文后切换任务，转录内容无法显示"bug**（workflow 多 agent 对抗验证根因）——根因：commit `9a96c09` 引入的按需加载只有 `watch(activeTab)` 一个触发点，且仅响应 activeTab **值变化**（无 immediate、无 `selectedTask.id` 依赖）；切换到已完成任务 B 时 activeTab 保持 'transcript' 不触发 watch，`include_content=true` 的完整内容请求永不发出 → 显示"暂无转录内容"（再点"原文"按钮也是 no-op，只能 tab 来回切或复制原文恢复）。同时发现次生 bug：`selectTask` 详情响应整体赋值会以 `_summary_overview` 截断版覆盖已加载的完整 summary（WS 重连/同任务重选时触发）。修复（`useTaskViewModel.ts`）：① watch 改为 `[activeTab, () => selectedTask.value?.id]` + `immediate: true`，切任务时原文 tab 自动加载新任务完整内容；② selectTask 详情响应改为按任务 id 字段级合并（浅拷贝响应对象，防止污染被复用 mock/响应），保留已加载的完整 transcript/summary；③ module 级 per-task 完整内容缓存 Map + in-flight Promise 去重，A→B→A 往返恢复且不重复请求；WS `task_update` 视为内容权威来源（处理中状态广播清缓存+递增版本，非处理中广播携带完整内容时无条件同步写缓存+递增版本；`fetchTaskFullContent` 响应带版本守卫，防止失效后在途旧响应重新污染缓存——对抗性测试发现并修复）；④ `downloadContent` 改 async 按需加载完整内容（避免下载到截断版/空文件），await 后复查任务 id；⑤ `copyContent` await 前捕获 taskId、await 后复查，修复 mid-await 切任务竞态（复制 B 内容或误弹"复制失败"）；⑥ `retryFailedParts` 失效 taskPartDetails 缓存。测试：前端新增 `useTaskViewModel.adversarial.test.ts`（22 用例：核心切任务回归 / 缓存隔离与失效 / 竞态 / 空转录边界 / 进行中任务 tab 重置 / 轻量不回归）+ 扩展 transcript 用例，全量 79 用例通过。
- 2026-08-08: **新增"仅转录原文"模式（AI 总结可选，feature）**——后端 `VALID_SUMMARY_MODES` 新增 `"none"` 枚举（deps.py 与 llm_worker.py 两处同步；`config.settings.py` 全局默认配置**故意**不支持 none，避免默认任务全变仅转录）；TranscriberWorker 转录完成后 `summary_mode=="none"` 时直接置终态（COMPLETED + progress=100，与 LLMWorker 完成态一致）并跳过 LLM 派发；video_downloader_worker 全部 4 个 LLM 派发点（multipart merge overview / batch 子分P / 单P字幕直取 / 多P字幕合并）同步跳过；multipart 链路 payload 透传 `summary_mode` 确认；已有 `POST /tasks/{task_id}/re-summarize` 天然支持给仅转录任务补总结（`_normalize_summary_mode` fallback 走任务已存模式，`_resolve_effective_mode` 把 none 兜底为 auto 判定）。前端：`SummaryMode` 类型加 `'none'`；Sidebar 提交表单新增醒目的"仅转录原文（跳过 AI 总结）"独立开关（琥珀色高亮 + PhFileText 图标 + "已开启/关闭"徽标，开启时提示"提交后不生成 AI 总结，之后可随时补总结"，`prevSummaryMode` 记住原模式一键恢复）；三种提交路径（URL/本地路径/文件上传）payload 自动携带 `summary_mode: 'none'`。测试：后端 `tests/test_summary_mode_none.py`（12 用例）+ `tests/test_adversarial_summary_mode_none.py`（12 用例：三模式不回归 / 大写 NONE / 终态与字段 / 多P e2e / 字幕直取 / re-summarize 补总结 / re-transcribe、re-download 组合）；前端 `Sidebar.transcriptOnly.test.ts`（2 用例）+ 对抗 payload 用例（4 用例：四种提交路径断言）。
- 2026-08-08: **E2E 验证（真实历史 URL）**——后端全量 `pytest tests`：195 passed / 1 failed（`test_create_task_publishes_task_created` 为既有失败：fixture 用非法 BV id `BV1test`，bilibili_api 校验 422，与本次改动无关）；前端 `vitest run` 全量 79 passed，`vue-tsc --noEmit` 零错误；ruff check 通过。真实 URL E2E：提交 `https://www.bilibili.com/video/BV1xwJx6iEdE` + `summary_mode=none` → 任务 TRANSCRIBING→COMPLETED（跳过 SUMMARIZING）、transcript 完整 464 字符、summary 为空；`re-summarize` → COMPLETED、summary 生成 881 字符。Playwright UI E2E（真实任务列表）：选任务 A → 切"原文"tab 显示 A 转录 → 切任务 B → 自动加载并显示 B 自己的转录（无陈旧、无"暂无转录内容"）→ A→B→A 往返恢复（缓存命中不重复请求）。测试任务已清理（删除任务 + temp 专属文件）。
- 2026-08-09: **模式 Tab 三选一：仅转录 | 标准 | Agent（默认仅转录）**——用户决策延续"仅转录原文"feature：Sidebar 提交表单的 segmented control 从二态（标准|Agent）改为三态，thumb 用显式 `Record<SummaryMode, string>` 类名映射（none `left-1` / standard `left-[calc(33.333%)]` / agent `left-[calc(66.667%)]`+渐变，杜绝枚举落空错位），宽度 `w-[calc(33.333%-6px)]`；仅转录（PhFileText、amber 点亮）+ 标准（PhLightning）+ Agent（PhBrain、白字+glow）；删除独立"仅转录原文"琥珀 toggle（toggleTranscriptOnly/prevSummaryMode/switchSummaryMode 清理），helper 文案保留为 none 时 Tab 下方提示；默认值从 'standard' 改为 **'none'**（Sidebar defineModel 与 composable 全局 ref 同步）。删除旧测试文件 Sidebar.transcriptOnly.test.ts，新增 Sidebar.summaryModeTab.test.ts（6 用例：三态 emit/thumb 类名互不相同/glow/toggle 不存在/默认 none）。
- 2026-08-09: **修复"切换任务原文丢失"用户可见复现的根因（stale dist 分发）+ 源码残留缺陷**——workflow 9 agent 对抗验证：修复 commit 5d777ca 只存在于源码，`frontend/dist`（gitignored）最后一次构建于 2026-08-08 07:57，早于修复约 7 小时，21010 持续服务旧 bundle（Starlette 每请求读盘不自愈，`start.sh` need_deploy() 因 dist 存在跳过构建）→ 用户看到的是修复前行为（"点 AI 总结再点原文才显示"）。处理：① 重建 dist（npm run build），21010 立即服务新包（无需重启）；② **start.sh need_deploy() 加固**：frontend/src 或 frontend/package.json mtime 新于 dist/index.html 时触发重建，防 stale-dist 复发；③ 源码残留修复：hasContent 判定（`''` 重置不再被 `!= null` 守卫当"已加载"，re-transcribe 重置后原文 tab 自愈重载且不无限重发）；过期在途 include_content=true 响应版本守卫扩展到 selectedTask 合并（旧内容不覆盖新内容，status/progress 照常）；**reTranscribe 确认弹窗**：summaryMode='none' 且目标任务有总结内容或 summary_mode ∈ {standard, agent}（模式信号兜底轻量详情在途/列表条目 summary 被剥离的窗口）时 confirm，取消中止——对抗测试发现的中危缺陷（静默清空总结）已修复；④ TaskMetaCard 'none' 渲染为"仅转录"（'auto' 保留兜底）；⑤ 后端 re_summarize_task 持久化/广播 summary_mode 从 'none' 改为实际有效模式（消除"none+SUMMARIZING"瞬时误导，worker payload 仍传 none 走 auto 兜底）。测试：前端对抗新增 21 用例（三态 payload 9 组合/循环切换/thumb 边界/历史值 auto 不 fall-through/hasContent 三态边界/并发去重/过期响应/confirm 攻击窗口/TaskMetaCard 渲染），后端对抗新增 11 用例（re_summarize none→auto 全链路/多分P/transcript='' 400/非法值回退/库内 none 透传），start.sh need_deploy 6 场景 mtime 沙箱验证；全量前端 108 passed、后端 206 passed（1 个 pre-existing 网络依赖失败）、vue-tsc 零错误、ruff 通过。
- 2026-08-09: **E2E 验证（21010 真实服务）**——Playwright：Tab 三选一可见、旧 toggle 已删除、默认仅转录 helper 文案出现、用户场景回归（选 A→原文→切 B 自动加载 B 转录、无陈旧）；真实 URL（BV1xwJx6iEdE）仅转录提交 → TRANSCRIBING→COMPLETED（跳过 SUMMARIZING）、transcript 455 字符完整、summary 空、mode=none；re-summarize → COMPLETED、summary 835 字符、持久化 summary_mode none→standard。测试任务已清理。
- 2026-08-09: **失败任务快速重跑 + 任务属性原网址可复制（feature）**——① Sidebar 任务列表 FAILED 行新增红色"快速重跑"按钮（@click.stop 不误触选中，manage 搜索视图对称），TaskInfoModal FAILED 状态新增"快速重跑"按钮，均复用 `POST /tasks/{id}/re-transcribe`（重置 PENDING 重新下载/转录/总结，按任务已存 summary_mode，含确认防护）；② TaskInfoModal"视频 URL"行显示**完整原网址文本**（truncate+悬停看全）+ 新标签打开 + PhCopy 复制按钮（成功 1.5s 内变 PhCheck"已复制"），file:// 本地任务不渲染 `<a>` 只显示路径+复制；TaskMetaCard"视频链接"同样加复制；新增 `utils/clipboard.ts`（优先 navigator.clipboard，降级 textarea+execCommand，兼容局域网 HTTP）。测试：前端新增 Sidebar.retryTask/TaskInfoModal/TaskMetaCard/clipboard 对抗测试 26 用例。
- 2026-08-09: **仅转录"总结标题"开关（feature，默认开启）**——选中"仅转录"（summary_mode='none'）时提交表单显示"总结标题"开关（role="switch" 默认开，文案"仅转录完成后自动生成标题"），standard/agent 隐藏；开启时仅转录任务完成后对转录全文调用 **LLM 生成标题**（`LLMWorker.generate_topic` 复用现有 `_llm_client`，即"api同款"；专用中文 prompt，转录截取前 12000 字符），写入任务 topic 字段，**不生成完整总结**；失败静默降级（warning，任务仍 COMPLETED）。链路：TaskCreate/LocalPathTaskCreate/upload Form 新增 `generate_topic: bool = True`（Pydantic 默认）→ TASK_CREATED payload → downloader/FileUploadWorker → transcriber none 分支（COMPLETED 广播后异步生成，不阻塞终态）/B站字幕直取/multipart merge 父任务（分P子任务跳过）；**开关持久化到 DB**（tasks 表 `generate_topic` BOOLEAN DEFAULT 1，`_ensure_schema` 幂等 ALTER + 老任务回填 True），re-transcribe/retry-failed-parts 按任务已存开关恢复（用户决策：重跑按原开关；重跑模式仍用当前 UI Tab 选择）。前端 5 条提交路径（URL/local-path/文件上传/分P/多文件批量）在 none 时显式携带 generate_topic，非 none 不发。测试：后端 test_generate_topic.py（18 用例）+ test_adversarial_generate_topic.py（16 用例：默认语义/显式覆盖/12000 边界/降级矩阵/字幕直取/持久化重跑恢复），前端 12 用例；全量前端 158 passed、后端 244 passed（1 个 pre-existing BV1test 网络失败）、ruff 全过。
- 2026-08-09: **E2E 验证（21010 真实服务，tmux 前台重启加载新后端）**——Playwright：Sidebar 失败任务行"快速重跑"按钮、Tab 仅转录时"总结标题"开关可见；真实 URL（BV1xwJx6iEdE）仅转录提交 → TRANSCRIBING→COMPLETED、transcript 464 字符、summary 空、**异步生成标题"危废污染现场探访：应急转运启动，来源调查中"**（DB 持久化 generate_topic=1 验证）。测试任务已清理。
- 2026-08-09: **⚠️ 数据丢失事件记录**——历史任务数据（65 COMPLETED 等 72 条）因实现子代理误删 `ShengWen.db` 丢失（误将真实库当作复现脚本杂散文件 rm，16:23:40；16:24:25 重建空库，无备份不可恢复）。根因与教训已记录（子代理禁止 rm/git clean 项目根文件、删除前确认文件身份、考虑启动备份）；功能代码不受影响（数据库为运行数据，重建后由新任务自然累积）。
- 2026-08-09: **数据库自动备份机制（feature，防数据丢失）**——新增 `DatabaseBackupLoop`（domain/database/backup_loop.py，模式同 StorageReclaimerLoop）：服务启动时首轮立即备份一次，之后按 `database.backup_interval_sec`（默认 86400s/1 天）循环，保留 `backup_keep_count`（默认 5）份轮转备份（`ShengWen.db.bak.1` 最新 ~ `.bak.5` 最老，超出删除最老）；使用 SQLite 在线 backup API 生成一致性快照（服务运行中可安全调用，`asyncio.to_thread` 不阻塞事件循环）；源库不存在/备份异常静默降级（warning，循环不崩溃）。配置：`DatabaseConfig` 新增两字段（settings.py + config/settings.json 同步）；接线：api.py 与 ShengWen-app.py 两处 lifespan 的 start/stop（与回收器对称）。`.gitignore` 新增 `*.db.bak.*`（保护备份不被 git clean 误删）。测试：tests/test_database_backup.py 8 用例（首轮生成/轮转顺序与删除最老/keep 下限/缺失降级/启停幂等/失败存活/快照一致性）。
- 2026-08-09: **CLAUDE.md 新增「强制需求」章节**——由 2026-08-09 数据丢失事件确立：① 禁止删除/移动/清空项目根任何文件（含 `rm`/`git clean` 全变体/`git reset --hard`/`git checkout .`）；② 删除前必须确认文件身份（`ShengWen.db` 数百 KB~MB 含任务数据 vs 杂散日志几 KB）；③ 运行时数据认知（cwd 相对路径 DB、模块级 `db = TaskDB()` 副作用、备份文件位置与恢复方式、前端改动必须重建 dist）；④ 子代理派发必须携带保护条款、不得自行清理项目根文件。
- 2026-08-10: **测试/生产双环境隔离（运维变更）**——用户指出此前在"生产环境（21010）上动代码"（开发中的代码直接跑在真实使用环境）。隔离方案：① 测试环境 = 本仓库 `/home/admin/projects/ShengWen`，端口改为 **21001**（config/settings.json app.port，服务在 tmux pane claude:shengwen 前台运行）；② 生产环境 = `/home/admin/projects/prod/ShengWen`（父目录 prod/ 下新建），git clone 本仓库 + checkout fix 分支 HEAD（633c6e1，快照冻结），`uv sync` + node_modules 复制 + `npm run build`（dist 18s）+ 复制 ShengWen.db/temp 数据；config/settings.json 从测试环境复制（含 LLM api_key 等真实配置）并保持 port=21010；服务在 tmux pane claude:shengwen-prod 前台运行，备份循环等机制自动生效（启动首轮备份已生成 .bak.1）。验证：两环境同时健康（21001 测试 / 21010 生产，生产已有局域网客户端访问）；测试环境 21001 真实 URL 全链路 E2E 通过（仅转录 → COMPLETED、transcript 453、summary 空、标题异步生成"危废现场跟进：转运启动，污染调查中"）；测试任务已清理。开发约定：开发/E2E 一律走 21001（vite dev 用 VITE_DEV_API_TARGET=http://127.0.0.1:21001）；生产代码更新 = 显式同步操作（测试环境全量验证后 fetch+checkout+build+重启）。
