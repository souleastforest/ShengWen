# 前端开发环境变量示例
#
# 用法：复制为 .env.local 后按需修改（.env.local 已被 .gitignore 忽略，不入库）。
# 变量名需与 vite.config.ts / 运行时代码实际读取的一致：
#   - VITE_DEV_API_TARGET：vite dev server 代理目标（vite.config.ts:9，默认 http://127.0.0.1:21001）
#   - VITE_API_BASE_URL：API 基础地址（useTaskViewModel.ts / shared/api/client.ts，缺省同源相对路径）
#   - VITE_WS_BASE_URL：WebSocket 基础地址（useTaskViewModel.ts，缺省 {location.origin}/ws）

# 后端代理目标（测试 21001 / 生产 21010）
VITE_DEV_API_TARGET=http://127.0.0.1:21001

# API 基础地址（如需直连非同源后端时设置，形如 http://127.0.0.1:21001/api）
# VITE_API_BASE_URL=

# WebSocket 基础地址（缺省 {当前 origin}/ws）
# VITE_WS_BASE_URL=
